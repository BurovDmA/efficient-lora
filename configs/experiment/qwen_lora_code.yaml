# @package _global_

defaults:
  - override /data: opencodeinstruct
  - override /model: causal_lm_lora
  - override /callbacks: lm_default
  - override /logger: mlflow
  - override /trainer: mps
  - _self_

tags: ["qwen", "lora", "sft", "code"]

seed: 42

trainer:
  max_epochs: 1
  log_every_n_steps: 5

test: False


